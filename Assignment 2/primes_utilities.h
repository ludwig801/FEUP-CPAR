#ifndef PRIMES_UTILITIES_H
#define PRIMES_UTILITIES_H

#include "types.h"

void printTimeSpent(TIME_VAR start, TIME_VAR end);

void printTimeSpent(double start, double end);

void printArray(const PRIMES_ARRAY & primes, INDEX_VAR length);

#endif
